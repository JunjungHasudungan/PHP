<?php

require('Produk');

/* class Buku extends Produk
{
    public $penulis;

    // nama, harga, penulis
    public function __construct($nama, $harga, $penulis)
    {
        parent::__construct($nama, $harga);
        $this->penulis = $penulis;
    }
}

$buku1 = new Buku("OOP PHP", 35000, "Nama kalian");

echo $buku1->getNama()->getHarga(); */